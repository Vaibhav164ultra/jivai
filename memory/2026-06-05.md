# 2026-06-05 - Daily Memory

- Created a minimal React component using Web Speech API (`SpeechRecognition.jsx`).
- Set up a Vite React project inside the `jiv ai` directory.
- Configured `App.jsx` to render the `SpeechRecognition` component.
- Started the development server at http://localhost:5173/.
- Modified the component to enable `interimResults = true` and display the live transcription directly on screen (with low-opacity interim text) to help with low microphone sensitivity.
- Optimized recognition delay by changing `continuous` to `false` and implementing an auto-restart loop in `onend`. This triggers faster finalization of transcribed segments on short pauses.
